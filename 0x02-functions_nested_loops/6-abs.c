#include "main.h"
/**
 * _abs - computes absolute value of integer
 * @n: integer to be computed
 * Return: integer
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
